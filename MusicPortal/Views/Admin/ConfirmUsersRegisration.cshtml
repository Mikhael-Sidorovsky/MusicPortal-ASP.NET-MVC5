@model IEnumerable<MusicPortal.Models.User>

@{
    ViewBag.Title = "Add user";
}

@if (Model.Count() > 0)
{
    Html.RenderPartial("_ConfirmUsersRegisrationPartial", Model);
}
else
{
    <h2>No new registration requests.</h2>
}

<div class="modal fade" id="modDialog" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div id="dialogContent" class="modal-dialog" role="document">

    </div>
</div>